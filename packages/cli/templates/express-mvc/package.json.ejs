{
  "name": "<%= name %>",
  "version": "1.0.0",
  "description": "Backend API generated with boilerplate-cli",
  "type": "module",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1"<% if (features.includes('cors')) { %>,
    "cors": "^2.8.5"<% } %><% if (features.includes('auth')) { %>,
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3"<% } %><% if (features.includes('logger')) { %>,
    "winston": "^3.11.0"<% } %><% if (features.includes('rateLimit')) { %>,
    "express-rate-limit": "^7.1.5"<% } %><% if (features.includes('validation')) { %>,
    "express-validator": "^7.0.1"<% } %><% if (orm === 'mongoose') { %>,
    "mongoose": "^8.0.3"<% } %><% if (orm === 'prisma') { %>,
    "@prisma/client": "^5.7.1"<% } %>
  },
  "devDependencies": {
    "nodemon": "^3.0.2"<% if (orm === 'prisma') { %>,
    "prisma": "^5.7.1"<% } %>
  },
  "keywords": [
    "api",
    "backend",
    "<%= framework %>",
    "<%= architecture %>"
  ]
}